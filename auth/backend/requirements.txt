# Requirements for MG-ERP Authentication Module

# FastAPI and web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.23
asyncpg==0.29.0
alembic==1.12.1

# Authentication and security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2  # explicit pin to suppress version attribute warning behavior

# Configuration and environment
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0

# Email validation and utilities
email-validator==2.1.0

# HTTP client for cross-service communication
httpx==0.25.2

# Development and testing
pytest==7.4.3
pytest-asyncio==0.21.1

# CORS is handled by FastAPI built-in middleware
# No additional CORS package needed